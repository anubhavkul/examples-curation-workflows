resources:
  - name: curation_gitrepo
    type: GitRepo
    configuration:
      gitProvider: githubcurationexample
      path: anubhavkul/examples-curation-workflows
      branches:
        include: main
